# 看板 HTML

静态模板（`*-dashboard.html`、`dashboard.html`）与脚本生成的 `*-live.html`（内嵌 JSON，避免 `file://` fetch 限制）。

由 `hybrid/Open-HybridSignoffDashboard.ps1`、`v4/Open-V4SignoffDashboard.ps1`、`surface/Open-SurfaceGateDashboard.ps1` 写入并打开。
